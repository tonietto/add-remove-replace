/* @flow */
import add from './add/add';
import remove from './remove/remove';
import replace from './replace/replace';

const addRemoveReplace = { add, remove, replace };

export { add, remove, replace };

export default addRemoveReplace;
